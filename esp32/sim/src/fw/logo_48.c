/* LVGL image asset (AgentDeck shield 48×48, ARGB8888) for the HUD/connection
 * card logo. Compiled as C so the const lv_image_dsc_t keeps external linkage
 * (a .cpp wrapper would give it internal linkage, hiding img_logo_48). */
#include "../../../src/ui/assets/logo_48.c"
